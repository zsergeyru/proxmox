name: Patch deploy wrapper pycache

on:
  push:
    branches:
      - fix-deploy-guest-pycache

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix-deploy-guest-pycache
      - name: Apply patch
        run: |
          python .github/fix-deploy-pycache.py
          git diff --check
          bash -n scripts/pve/setup/lib/70-tooling.sh
          bash scripts/pve/setup/tests/test-deploy-runtime-contract.sh
          git config user.name github-actions[bot]
          git config user.email 41898282+zsergeyru@users.noreply.github.com
          git add scripts/pve/setup/lib/70-tooling.sh scripts/pve/setup/lib/00-common.sh scripts/pve/setup/tests/test-deploy-runtime-contract.sh
          git commit -m "fix: не считать pycache загрязнением deploy checkout"
          git push origin HEAD:fix-deploy-guest-pycache
