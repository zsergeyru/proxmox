# План нумерации VMID / CTID

**Тип:** спецификация  
**Статус:** проектируется  
**Назначение:** задать единую функциональную нумерацию постоянных VM и LXC проекта.

Этот документ отвечает на вопрос:

> Как выбирается VMID или CTID новой постоянной гостевой системы и какие диапазоны закреплены за разными областями проекта?

Документ описывает целевое правило нумерации. Фактические номера уже существующих гостей могут временно отличаться до отдельной перенумерации.

## Содержание

- [1. Правило номера](#1-правило-номера)
- [2. Функциональные области](#2-функциональные-области)
- [3. Управление инфраструктурой](#3-управление-инфраструктурой)
- [4. Технические объекты](#4-технические-объекты)
- [5. Применение правила](#5-применение-правила)
- [6. Связанные документы](#6-связанные-документы)

## 1. Правило номера

Для постоянных гостевых систем используется трёхзначный VMID/CTID:

~~~text
XYZ

X — функциональная область
Y — служба внутри области
Z — экземпляр службы
~~~

Основной экземпляр службы получает номер с последней цифрой `0`.

Примеры:

~~~text
210 — основной экземпляр службы
211 — второй экземпляр той же службы
212 — третий экземпляр той же службы
~~~

Поэтому основные VM/LXC обычно получают VMID/CTID, кратный `10`.

Номера `100`, `200`, `300` и другие номера, кратные `100`, не используются для обычных гостевых систем. Они обозначают границы функциональных областей.

## 2. Функциональные области

| Диапазон | Назначение |
|---|---|
| `100–190` | сеть и сетевые службы |
| `200–290` | умный дом и домашняя автоматизация |
| `300–390` | приложения и разработка |
| `400–490` | AI и голосовые службы |
| `500–590` | видео и наблюдение |
| `600–690` | хранение и резервные копии |
| `700–790` | наблюдение за состоянием и журналы |
| `800–890` | резерв для будущей функциональной области |
| `900–990` | управление инфраструктурой |

Текущие основные назначения:

~~~text
110 — network-gateway
210 — ha-main
220 — automation-services
310 — dev-services
320 — app-services
410 — ai-control
420 — ai-services
510 — frigate
710 — monitoring
910 — infra-manager
~~~

Дополнительные экземпляры одной службы занимают следующие номера после основного. Например, для Home Assistant:

~~~text
210 — ha-main
211 — ha-test
212 — ha-flat2
~~~

В области AI закреплены две основные службы:

~~~text
410 — ai-control
420 — ai-services
~~~

Номера `411–419` резервируются для дополнительных или проверочных экземпляров AI Control.

Номера `421–429` резервируются для дополнительных или проверочных экземпляров общих AI- и голосовых служб.

Остальные номера диапазона `400–490` распределяются между отдельными AI-службами по мере утверждения их назначения.

В области наблюдения закреплён основной гость:

~~~text
710 — monitoring
~~~

Номера `711–719` резервируются для дополнительных или проверочных экземпляров системы наблюдения.

## 3. Управление инфраструктурой

Диапазон `900–990` предназначен для гостевых систем, которые создают, изменяют, обслуживают или восстанавливают саму инфраструктуру проекта.

Первым закреплённым объектом этого диапазона является:

~~~text
910 — infra-manager
~~~

`910 infra-manager` — постоянный управляющий LXC проекта.

В нём выполняются средства, через которые создаётся, изменяется, обслуживается и удаляется управляемая инфраструктура.

Его собственный жизненный цикл является особым и не входит в обычное управление гостями через OpenTofu.

Номера `911–919` резервируются для дополнительных или проверочных экземпляров той же управляющей среды, если они когда-либо понадобятся.

## 4. Технические объекты

Четырёхзначные технические VMID не входят в функциональную трёхзначную нумерацию постоянных служб.

Например:

~~~text
9000 — базовый шаблон Debian VM
~~~

Такие номера предназначены для шаблонов, сборочных VM и других технических объектов, которые не являются постоянными прикладными гостями.

Технические объекты не должны занимать номера из функционального диапазона `100–990`.

## 5. Применение правила

Этот документ задаёт целевую нумерацию проекта.

Новый постоянный гость должен получать номер из диапазона, соответствующего его основной функции.

Если существующий каталог в `infrastructure/guests/` или фактическая VM/LXC пока имеет номер, не соответствующий этому правилу, это считается переходным состоянием.

Перенумерация существующих объектов выполняется отдельно, потому что изменение VMID/CTID может затрагивать:

- каталог гостя;
- `guest.yaml`;
- сетевую адресацию;
- состояние OpenTofu;
- ссылки из других частей проекта;
- фактический объект в Proxmox.

Само наличие целевого номера в этом документе не означает автоматическую перенумерацию существующего объекта.

## 6. Связанные документы

- [`300-overview.md`](300-overview.md) — общий обзор гостевых систем.
- [`310-guest-state.md`](310-guest-state.md) — построение требуемого состояния гостя.
- [`320-guest-manifest.md`](320-guest-manifest.md) — справочник `guest.yaml`.
- [`330-guest-lifecycle.md`](330-guest-lifecycle.md) — жизненный цикл обычной управляемой VM/LXC.
- [`390-decisions.md`](390-decisions.md) — причины ключевых решений гостевого контура.
- [`../../infrastructure/guests/README.md`](../../infrastructure/guests/README.md) — каталоги конкретных гостевых систем.
