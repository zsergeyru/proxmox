# Требуемое состояние гостевой системы

**Тип:** спецификация  
**Статус:** действующий  
**Назначение:** определить источники, параметры, правила построения и проверки требуемого состояния VM/LXC.

Этот документ отвечает на вопрос:

> Как в проекте описывается требуемое состояние конкретной гостевой системы и как из исходных файлов получается итоговое effective state?

Документ не описывает порядок применения изменений к Proxmox. Контракт PLAN/APPLY и жизненный цикл команды `deploy-guest` относятся к отдельной спецификации развёртывания.

## Содержание

- [1. Назначение и модель состояния](#1-назначение-и-модель-состояния)
- [2. Источники и построение состояния](#2-источники-и-построение-состояния)
- [3. Параметры гостевой системы](#3-параметры-гостевой-системы)
- [4. Правила и ограничения](#4-правила-и-ограничения)
- [5. Проверка состояния](#5-проверка-состояния)
- [6. Связанные документы](#6-связанные-документы)

## 1. Назначение и модель состояния

### 1.1. Назначение документа

Для управляемой VM/LXC проект хранит не готовую конфигурацию Proxmox, а исходное описание требуемого состояния.

Оно должно быть:

- компактным;
- проверяемым;
- воспроизводимым;
- однозначно преобразуемым в итоговое состояние;
- пригодным для сравнения с фактическим состоянием Proxmox.

Основной индивидуальный файл гостя:

```text
guests/<VMID>-<name>/guest.yaml
```

Общие значения и профили находятся в:

```text
guests/defaults.yaml
```

Машинные ограничения формата задаются схемами в `schemas/`.

### 1.2. Что считается требуемым состоянием

Требуемое состояние определяет, каким должен быть объект VM/LXC с точки зрения проекта.

К нему относятся, в частности:

- идентификатор и имя;
- тип гостевой системы;
- размещение;
- процессор, память и диск;
- сеть;
- правила запуска;
- административный management-контур;
- явно запрошенная первоначальная подготовка;
- параметры, специфичные для VM или LXC.

Работающая VM/LXC в Proxmox хранит фактическое состояние, но сама по себе не становится источником требований.

Изменение требуемого состояния выполняется через соответствующие файлы проекта.

### 1.3. Исходное и итоговое состояние

В проекте различаются два представления состояния.

**Исходное состояние** — компактные данные, которые человек редактирует в Git:

```text
guests/defaults.yaml
+
профиль
+
guests/<VMID>-<name>/guest.yaml
```

**Effective state** — полное итоговое состояние после наследования, объединения, нормализации и вычисления производных значений.

Общая схема:

```text
исходные данные
→ schema validation
→ resolver
→ effective state
→ проверка effective state
```

Средства проверки и развёртывания должны использовать одно и то же effective state и не реализовывать собственные параллельные правила его построения.

## 2. Источники и построение состояния

### 2.1. Источники состояния

Требуемое состояние строится из трёх основных источников.

`guests/defaults.yaml` содержит:

- общие значения проекта;
- параметры, одинаковые для большинства гостей;
- определения профилей VM/LXC.

Профиль определяет типовую основу конкретного класса гостевых систем.

Например:

```yaml
profiles:
  debian-vm:
    type: vm
    template_vmid: 9000

  debian-lxc:
    type: lxc
    ostemplate: local:vztmpl/debian-13-standard

  debian-lxc-docker:
    type: lxc
    ostemplate: local:vztmpl/debian-13-standard
    features:
      - container-host
```

Индивидуальный `guest.yaml` содержит параметры конкретной VM/LXC и осознанные отклонения от общих значений.

Обычный гость с `profile` не должен копировать в свой `guest.yaml` значения, которые уже однозначно приходят из `defaults.yaml` или выбранного профиля.

### 2.2. Порядок объединения и переопределения

Порядок построения состояния фиксирован:

```text
defaults
→ profile
→ guest.yaml
→ специальные правила
→ вычисляемые значения
→ effective state
```

Словари объединяются рекурсивно.

Более позднее простое значение заменяет более раннее.

Избыточное повторение унаследованного значения нежелательно: оно усложняет чтение манифеста и может скрывать последующие изменения общих настроек.

### 2.3. Специальные и вычисляемые значения

Не все поля следуют обычному наследованию.

Некоторые параметры разрешено задавать только конкретному гостю.

К таким индивидуальным возможностям относятся элементы списков `management` и `bootstrap`.

Они не должны появляться в общих defaults или профилях.

Некоторые значения вообще не хранятся в обычном `guest.yaml`, а вычисляются из других параметров.

Главный пример — административный IPv4.

Если индивидуальное переопределение отсутствует, адрес вычисляется из VMID и центральной management-подсети.

### 2.4. Итоговое effective state

Общий resolver проекта:

```text
scripts/guest_config.py
```

строит deterministic effective state.

Для гостя с `profile` итоговое состояние должно содержать все обязательные параметры, необходимые следующим слоям проекта.

В частности, resolver:

- объединяет defaults, профиль и индивидуальный манифест;
- нормализует список индивидуальных management-возможностей;
- вычисляет административный IP;
- проверяет и нормализует запрошенный Bootstrap;
- возвращает запрошенные Bootstrap-инструменты в каноническом порядке.

Машинный формат итогового состояния проверяется схемой:

```text
schemas/guest-effective.schema.yaml
```

Effective state является интерфейсом между описанием гостя и средствами, которые сравнивают или применяют это состояние.

## 3. Параметры гостевой системы

Этот раздел является подробным справочником параметров требуемого состояния.

Для каждого параметра указано:

- где он используется;
- для чего нужен;
- что происходит, если его задать;
- что происходит, если его не задавать;
- пример применения.

Здесь описывается смысл отдельных параметров. Правила, связывающие несколько параметров между собой, собраны в разделе 4.

### 3.1. Основные параметры

#### 3.1.1. `schema_version`

**Тип:** integer с фиксированным допустимым значением.

В обычном `guest.yaml` используется:

```yaml
schema_version: 10
```

Для всего guest-state используется единая версия контракта. Поэтому и обычный `guest.yaml`, и `guests/defaults.yaml`, и effective state используют один номер:

```yaml
schema_version: 10
```

Параметр нужен для того, чтобы валидатор однозначно понимал, по какой версии общего guest-state контракта проверять файл.

Если указано поддерживаемое значение, файл проверяется по соответствующему текущему контракту.

Если поле отсутствует или указана другая версия, schema validation завершается ошибкой. Автоматического преобразования старой версии в новую нет.

#### 3.1.2. `vmid`

**Тип:** integer от `100` до `999`.

Используется в каждом `guest.yaml`.

Пример:

```yaml
vmid: 311
```

Параметр задаёт VMID для VM или CTID для LXC. Он также участвует в имени каталога и по умолчанию используется для вычисления management IPv4.

Для:

```text
guests/311-dev-services/
```

манифест должен содержать:

```yaml
vmid: 311
```

Если `vmid` задан, но не совпадает с числовой частью имени каталога, проверка репозитория завершается ошибкой.

Если тот же VMID использован ещё одним гостем, проверка также завершается ошибкой.

Если поле не задано, исходный манифест не проходит schema validation.

#### 3.1.3. `name`

**Тип:** string вида `[a-z0-9][a-z0-9-]*`.

Используется в каждом `guest.yaml`.

Пример:

```yaml
name: dev-services
```

Имя идентифицирует гостя внутри проекта и должно совпадать с частью имени каталога после `<VMID>-`.

Для:

```text
311-dev-services
```

допустимо:

```yaml
name: dev-services
```

Если имя в манифесте не совпадает с каталогом, валидатор выдаёт ошибку.

Если поле отсутствует, манифест не проходит schema validation.

#### 3.1.4. `description`

**Тип:** непустая string.

Используется в каждом `guest.yaml`.

Пример:

```yaml
description: Ansible, Semaphore, Git, CI and development services
```

Поле кратко объясняет назначение гостя. Это описание для человека, но оно также участвует в проверках качества манифеста.

Для гостя с `profile` текст с маркерами незавершённости вроде `todo`, `tbd`, `unknown` и аналогичными формулировками вызывает предупреждение.

Если `description` отсутствует или пуст, исходный манифест не проходит schema validation.

#### 3.1.5. `profile`

**Тип:** string — имя профиля из `guests/defaults.yaml`.

Пример:

```yaml
profile: debian-lxc-docker
```

Параметр выбирает типовую основу гостя.

Например, профиль:

```yaml
profiles:
  debian-lxc-docker:
    type: lxc
    ostemplate: local:vztmpl/debian-13-standard
    features:
      - container-host
```

передаёт гостю тип `lxc` и параметры LXC.

Наличие `profile` означает, что гость участвует в универсальном контуре построения effective state и может передаваться `deploy-guest`.

Если профиль указан, он должен существовать в `guests/defaults.yaml`. Неизвестное имя профиля приводит к ошибке resolver.

Если `profile` отсутствует, объект может оставаться описанным в Git, но полное effective state для него не строится и универсальный deploy-контур не должен им управлять.

#### 3.1.6. `node`

**Тип:** непустая string.

Определяет PVE-узел, на котором должен находиться гость.

Общее значение сейчас задаётся в `guests/defaults.yaml`:

```yaml
defaults:
  node: pve
```

Обычному гостю не нужно повторять:

```yaml
node: pve
```

Если `node` не задан в индивидуальном `guest.yaml`, он наследуется из defaults или профиля.

Если гостю осознанно нужен другой узел, поле можно переопределить:

```yaml
node: pve2
```

Такое отклонение от общего значения валидатор отмечает предупреждением.

Если после объединения значение `node` отсутствует, effective state гостя с `profile` не проходит проверку.

#### 3.1.7. `protection`

**Тип:** boolean.

Определяет желаемое состояние встроенной PVE protection для VM/LXC.

Общее значение:

```yaml
defaults:
  protection: true
```

Поэтому обычный гость с `profile` защищён по умолчанию. Это дополнительный предохранитель от случайных разрушительных операций на уровне Proxmox; параметр не ограничивает настройку гостевой ОС по SSH.

Если для конкретного гостя protection осознанно не требуется, допускается индивидуальное переопределение:

```yaml
protection: false
```

Такое переопределение валидатор отмечает предупреждением, потому что оно снимает стандартную защиту проекта.

Если параметр отсутствует во всех источниках, effective state не соответствует действующей схеме.

#### 3.1.8. `pve_management`

**Тип:** boolean.

Определяет, разрешено ли AI-агенту управлять VM/LXC **как объектом Proxmox** через PVE API/MCP.

Общее значение:

```yaml
defaults:
  pve_management: true
```

При `pve_management: true` гость должен находиться в фиксированном PVE pool `managed`. Права `ai-agent@pve!infra` на управление жизненным циклом и конфигурацией VM/LXC выдаются через ACL этого pool.

Редкое индивидуальное исключение задаётся так:

```yaml
pve_management: false
```

В этом случае гость не должен находиться в pool `managed`, поэтому AI-агент не получает обычные PVE/MCP-права на его запуск, остановку, изменение виртуального оборудования, снапшоты или удаление как объекта Proxmox.

`pve_management` **не управляет SSH-доступом**. При `false` AI и остальные участники management SSH могут продолжать администрировать гостевую ОС, если их ключи разрешены общим SSH-контрактом. Deployer также сохраняет собственные PVE-права, необходимые deploy-контуру.

Имя pool `managed` является фиксированной внутренней частью PVE-контракта и не задаётся в `guest.yaml`. Списка произвольных pool в guest state нет.

Индивидуальное `pve_management: false` валидатор отмечает предупреждением как осознанное отклонение от общей политики.

> **Статус реализации:** defaults, schemas, resolver и validator используют `pve_management`. Текущий `deploy-guest` всё ещё относится к старому контракту `placement.pool` и будет заменён при отдельной полной переработке deploy-контура.

### 3.2. Ресурсы

Раздел `resources` описывает ресурсы **конкретной** VM/LXC.

Принцип разделения ответственности:

```text
profile
→ определяет тип гостя и способ его создания

resources
→ определяет ресурсы конкретного экземпляра
```

Поэтому профили не должны задавать индивидуальные `cores`, `memory_mb`, `ballooning_mb`, `swap_mb` или `disk_size_gb`.

Общие инфраструктурные значения, одинаковые для большинства гостей, могут находиться в `defaults`. Основной пример — `disk_storage`.

Целевая структура ресурсов плоская:

```yaml
resources:
  cores: 2
  memory_mb: 4096
  ballooning_mb: 2048
  swap_mb: 1024
  disk_size_gb: 32
  disk_storage: local-lvm
```

Конкретная VM или LXC содержит только применимые к ней параметры.

> **Статус реализации:** schemas, resolver, validator и guest-манифесты уже используют эту плоскую модель ресурсов. Старый `deploy-guest` будет переведён на неё при отдельной полной переработке.

#### 3.2.1. `resources.cores`

**Тип:** integer, минимум `1`.

Задаёт количество CPU, доступных конкретному гостю.

Для VM значение определяет число виртуальных CPU.

Для LXC значение определяет число CPU cores, доступных контейнеру средствами Proxmox.

Пример:

```yaml
resources:
  cores: 2
```

Для любого гостя с `profile` параметр обязателен непосредственно в его `guest.yaml`.

Параметр не должен задаваться профилем: разные гости одного профиля могут иметь разное количество CPU.

Если поле отсутствует у гостя с `profile`, конфигурация должна считаться неполной и не проходить проверку.

#### 3.2.2. `resources.memory_mb`

**Тип:** integer, минимум `256`.

Задаёт объём оперативной памяти в MiB.

Для VM это объём RAM виртуальной машины.

Для LXC это лимит памяти контейнера.

Пример:

```yaml
resources:
  memory_mb: 4096
```

Для любого гостя с `profile` параметр обязателен непосредственно в его `guest.yaml`.

Параметр не должен задаваться профилем.

Если поле отсутствует, конфигурация гостя с `profile` должна завершать проверку ошибкой.

#### 3.2.3. `resources.ballooning_mb`

**Тип:** integer, минимум `0`.

Параметр применяется **только к VM** и используется только тогда, когда для конкретной VM требуется явно управлять ballooning.

Обычной VM этот параметр не требуется.

Пример:

```yaml
resources:
  cores: 2
  memory_mb: 4096
  ballooning_mb: 2048
  disk_size_gb: 32
```

Если `ballooning_mb` указан, проект должен сравнивать и применять соответствующий параметр VM.

Если поле отсутствует, это не является ошибкой и означает, что проект не задаёт отдельного требования к ballooning этой VM.

Значение не может превышать `memory_mb`.

Например:

```yaml
resources:
  memory_mb: 4096
  ballooning_mb: 8192
```

должно завершать проверку ошибкой.

Для LXC параметр запрещён. Наличие `ballooning_mb` у LXC должно считаться ошибкой конфигурации.

Параметр не должен задаваться в профиле или общих defaults: это редкая индивидуальная настройка конкретной VM.

#### 3.2.4. `resources.swap_mb`

**Тип:** integer, минимум `0`.

Параметр применяется **только к LXC** и задаёт объём swap контейнера в MiB.

Пример:

```yaml
resources:
  cores: 2
  memory_mb: 4096
  swap_mb: 1024
  disk_size_gb: 32
```

Значение `0` является допустимым и означает явное требование создать LXC без swap:

```yaml
resources:
  swap_mb: 0
```

Для LXC с `profile` параметр должен быть задан непосредственно в `guest.yaml`.

Параметр не должен задаваться профилем: размер swap является настройкой конкретного контейнера.

Если `swap_mb` отсутствует у LXC с `profile`, проверка должна завершаться ошибкой.

Для VM параметр запрещён. Наличие `swap_mb` у VM должно считаться ошибкой конфигурации.

#### 3.2.5. `resources.disk_size_gb`

**Тип:** integer, минимум `1`.

Определяет требуемый размер основного диска VM или root filesystem LXC в GiB.

Пример:

```yaml
resources:
  disk_size_gb: 32
```

Для любого гостя с `profile` параметр обязателен непосредственно в его `guest.yaml`.

Параметр не должен задаваться профилем, потому что размер диска относится к конкретному экземпляру.

Для VM значение описывает требуемый размер основного диска после создания из шаблона.

Для LXC значение используется как требуемый размер root filesystem.

Увеличение размера может выполняться автоматически средствами развёртывания.

Уменьшение диска не должно выполняться автоматически и должно приводить к блокировке изменения.

#### 3.2.6. `resources.disk_storage`

**Тип:** непустая string.

Определяет PVE storage для основного диска VM или root filesystem LXC.

Это инфраструктурный параметр, который обычно одинаков для большинства гостей, поэтому его нормальное место — общие defaults.

Целевая запись:

```yaml
defaults:
  resources:
    disk_storage: local-lvm
```

Обычный `guest.yaml` тогда не повторяет storage:

```yaml
resources:
  cores: 2
  memory_mb: 4096
  disk_size_gb: 32
```

Если конкретному гостю требуется другое хранилище, допускается индивидуальное переопределение:

```yaml
resources:
  disk_storage: fast-storage
```

Если `disk_storage` отсутствует и у гостя, и в defaults, итоговое effective state должно считаться неполным.

Профиль не должен определять storage конкретного гостя: выбор общего хранилища относится к инфраструктурным defaults, а индивидуальное исключение — к `guest.yaml`.

#### 3.2.7. Проверка применимости ресурсов

Тип гостя определяется выбранным профилем, после чего validator должен дополнительно проверять применимость resource-полей.

Для VM обязательны:

```text
resources.cores
resources.memory_mb
resources.disk_size_gb
```

Для VM разрешён, но не обязателен:

```text
resources.ballooning_mb
```

Для VM запрещён:

```text
resources.swap_mb
```

Для LXC обязательны:

```text
resources.cores
resources.memory_mb
resources.swap_mb
resources.disk_size_gb
```

Для LXC запрещён:

```text
resources.ballooning_mb
```

Для обоих типов итоговое effective state должно содержать:

```text
resources.disk_storage
```

Значение может прийти из defaults или быть индивидуально переопределено.

Дополнительно проверяются:

- `cores >= 1`;
- `memory_mb >= 256`;
- `disk_size_gb >= 1`;
- `swap_mb >= 0`;
- `ballooning_mb >= 0`;
- `ballooning_mb <= memory_mb`;
- storage должен существовать и быть доступен на выбранном PVE node при развёртывании.

Параметр `resources.cpu.type` в целевой модели ресурсов отсутствует.

Он не должен храниться как декларативное поле, пока средство развёртывания не умеет им управлять. Если управление CPU model для VM действительно понадобится, для него должен быть введён отдельный осмысленный контракт.

Параметры CPU limit/weight, IOPS, bandwidth limits, NUMA, hugepages, дополнительные диски и passthrough-устройства в базовый `resources` сейчас не добавляются.

Они должны появляться только при реальной необходимости и с одновременной реализацией проверки и применения.

### 3.3. Сеть

Сетевой раздел описывает базовую management-сеть гостевой системы.

Для обычного управляемого гостя сеть почти полностью определяется общими defaults. В индивидуальном `guest.yaml` сетевой блок нужен только для редкого осознанного исключения по IPv4.

Целевая общая конфигурация:

```yaml
defaults:
  network:
    bridge: vmbr0
    subnet: 192.168.0.0/16
    gateway: 192.168.1.1
```

Обычный `guest.yaml` вообще не содержит раздел `network`.

> **Статус реализации:** schemas, resolver, validator и guest-манифесты уже используют эту модель без `network.addressing` и с простым `network.ipv4`. Старый `deploy-guest` будет переведён отдельно.

#### 3.3.1. `network.bridge`

**Тип:** непустая string.

Определяет PVE bridge для основного management-интерфейса гостя.

Центральное значение:

```yaml
defaults:
  network:
    bridge: vmbr0
```

В текущем базовом контракте bridge задаётся только в `defaults`.

Индивидуальный `guest.yaml` не должен переопределять `network.bridge`.

Причина проста: первая версия универсального развёртывания работает с одним общим management bridge. Поддержку других bridge, VLAN или нескольких интерфейсов следует вводить только вместе с отдельной моделью сетевых интерфейсов.

Если `bridge` отсутствует в общих defaults, effective state управляемого гостя считается неполным.

#### 3.3.2. `network.subnet`

**Тип:** строка IPv4 network.

Задаётся только в центральных `defaults`.

Пример:

```yaml
defaults:
  network:
    subnet: 192.168.0.0/16
```

Подсеть используется для вычисления management IPv4 и его prefix.

В текущей модели подсеть должна быть IPv4 `/16`, потому что проект использует фиксированное правило вычисления адреса из VMID.

Индивидуальный `guest.yaml` не может переопределить `network.subnet`.

Если параметр отсутствует, имеет неверный формат или не является поддерживаемой management-подсетью, effective state не строится.

#### 3.3.3. `network.gateway`

**Тип:** строка IPv4 address.

Задаётся только в центральных defaults.

Пример:

```yaml
defaults:
  network:
    gateway: 192.168.1.1
```

Gateway должен находиться внутри `network.subnet` и не может совпадать с network или broadcast address.

Все обычные управляемые гости получают один и тот же gateway из defaults.

Индивидуальный `guest.yaml` не должен задавать собственный gateway.

Если в будущем появится гость с другой L3-сетью или отдельным gateway, это потребует расширенной модели сетевых интерфейсов, а не одиночного override этого поля.

#### 3.3.4. `network.ipv4`

В исходном `guest.yaml` это **необязательная bare IPv4 string без prefix**.

Поле используется только как индивидуальное исключение из стандартной VMID-адресации.

Пример:

```yaml
network:
  ipv4: 192.168.8.50
```

Если поле отсутствует, management IPv4 вычисляется автоматически из VMID и центральной `network.subnet`.

Для VMID `311` и подсети:

```text
192.168.0.0/16
```

получается:

```text
192.168.3.11
```

Отдельный параметр выбора алгоритма адресации не нужен: VMID-адресация является правилом проекта.

Если `network.ipv4` задан, resolver использует этот адрес вместо вычисленного.

Prefix в исходном `guest.yaml` не указывается. Он берётся из `network.subnet`.

Поэтому допустимо:

```yaml
network:
  ipv4: 192.168.8.50
```

и недопустимо:

```yaml
network:
  ipv4: 192.168.8.50/16
```

В effective state адрес уже содержит prefix:

```yaml
network:
  ipv4: 192.168.8.50/16
```

Индивидуальный адрес должен:

- находиться внутри management-подсети;
- не совпадать с gateway;
- не быть network или broadcast address;
- не дублировать адрес другого гостя.

Если override совпадает с автоматически вычисляемым адресом, он избыточен и должен отмечаться предупреждением.

Если override отличается от VMID-адресации, это допустимое осознанное исключение.

#### 3.3.5. Границы базового сетевого контракта

В базовый guest contract сейчас не входят:

- несколько сетевых интерфейсов;
- индивидуальный bridge;
- VLAN tag/trunk;
- индивидуальный gateway;
- MTU;
- rate limit;
- firewall-флаг интерфейса;
- индивидуальная MAC-адресация;
- отдельные DNS/search-domain параметры;
- IPv6-конфигурация гостя.

Это не запрещает добавить такие возможности позже.

Но они должны появляться только при реальном сценарии использования и, если потребуется несколько интерфейсов или сетевых сегментов, через отдельную явную модель интерфейсов, а не через разрастание текущего простого `network`.

### 3.4. Запуск

Запуск гостя после создания и автоматический запуск вместе с PVE — разные вещи.

В требуемом состоянии конкретного гостя нужен только параметр, который описывает постоянную политику Proxmox.

#### 3.4.1. `boot.onboot`

**Тип:** boolean.

Определяет, должен ли Proxmox автоматически запускать гостя при старте PVE.

Общая политика:

```yaml
defaults:
  boot:
    onboot: true
```

Обычный гость наследует `true` и не повторяет это значение.

Если конкретный объект не должен автоматически запускаться вместе с PVE:

```yaml
boot:
  onboot: false
```

это является осознанным индивидуальным override.

Если `onboot` отсутствует и в guest, и в defaults, effective state управляемого гостя считается неполным.

Параметры порядка запуска, задержек и shutdown timeout в базовый контракт пока не добавляются. Они должны появиться только при реальной необходимости управлять зависимостями запуска между гостями.

#### 3.4.2. Запуск после развёртывания

Отдельного параметра `boot.start_after_deploy` в целевой модели нет.

Управляемый `deploy-guest` сам отвечает за жизненный цикл развёртывания:

```text
создание или изменение гостя
→ запуск
→ проверка административного SSH
→ первоначальные post-deploy действия
→ финальная проверка
```

Поэтому запуск после deploy является частью контракта средства развёртывания, а не настраиваемым свойством конкретной VM/LXC.

Если в будущем потребуется поддержать сценарий «создать гостя и оставить выключенным», для него должен быть спроектирован отдельный понятный lifecycle, включая способ финальной проверки без работающего SSH.

> **Статус реализации:** `boot.start_after_deploy` уже удалён из schemas, resolver и guest-манифестов. Он остаётся только в старом `deploy-guest`, который будет переработан отдельно.

### 3.5. Management

Management описывает дополнительные права и возможности конкретного управляемого гостя.

Административный SSH при этом не является настраиваемым параметром гостя.

Для любого гостя с `profile` действует фиксированный management-контракт:

```text
SSH user: root
SSH port: 22
```

Поэтому `management.ssh.user` и `management.ssh.port` в целевой модели отсутствуют.

Deployer, синхронизация management-ключей и остальные средства управления должны использовать `root:22` как единое правило проекта.

> **Статус реализации:** defaults, schemas, resolver, validator, `sync-management-keys` и guest-манифесты уже используют новый management-контракт. Старый `deploy-guest` будет переведён отдельно.

В целевом source-контракте `management` — это список дополнительных management-возможностей.

Пример:

```yaml
management:
  - ssh_identity
  - project_repo_read
```

Допустимы только:

```text
ssh_identity
project_repo_read
```

Если гостю не требуется ни одна дополнительная management-возможность, поле `management` вообще не создаётся.

Пустой список, boolean-значения, неизвестные элементы и дубликаты не допускаются.

В effective state `management` также должен быть представлен каноническим списком запрошенных возможностей, а не набором boolean-полей.

#### 3.5.1. `ssh_identity`

Элемент:

```yaml
management:
  - ssh_identity
```

означает, что этому гостю требуется собственная **исходящая** management SSH identity для административного доступа к другим управляемым гостям.

Это не ключ, которым PVE подключается к данному гостю.

Возможность задаётся только в индивидуальном `guest.yaml` и требует `profile`, потому что создание, регистрация и проверка identity относятся к управляемому deploy-контуру.

Если элемента `ssh_identity` в списке нет, собственная исходящая management SSH identity этому гостю не требуется.

#### 3.5.2. `project_repo_read`

Элемент:

```yaml
management:
  - project_repo_read
```

означает, что гостю нужен управляемый read-only доступ к приватному репозиторию проекта.

Элемент не содержит имя ключа, путь к private key, URL репозитория или произвольные Git credentials. Он только включает заранее определённую проектную возможность.

Возможность задаётся только в индивидуальном `guest.yaml` и требует `profile`.

Если элемента `project_repo_read` в списке нет, проектный read-only Git-доступ этому гостю не требуется.

`project_repo_read` не устанавливает Git client. Наличие Git как программы определяется отдельно через Bootstrap.

Точный APPLY/REMOVE lifecycle management-возможностей относится к спецификации `deploy-guest`.

### 3.6. Bootstrap

Bootstrap используется только для минимальной первоначальной установки инфраструктурных инструментов внутри конкретной VM/LXC.

**Тип:** список заранее определённых Bootstrap-инструментов.

Целевая форма:

```yaml
bootstrap:
  - git
  - docker
  - ansible
```

Допустимы только:

```text
git
docker
ansible
```

Если Bootstrap этому гостю не нужен, поле `bootstrap` вообще не создаётся.

Пустой список, произвольные имена, значения `true/false`, package lists и shell-команды в source-контракт не входят.

> **Статус реализации:** schemas, resolver, validator, тесты и guest-манифесты уже используют список `bootstrap`. Старый `deploy-guest` пока использует прежний Bootstrap-контракт и будет переработан отдельно.

Базовая техническая готовность больше не является выбираемой capability.

Для каждого управляемого Debian-гостя deploy-контур сам должен обеспечить необходимые базовые предпосылки для дальнейшего управления, например Python, CA certificates и `rsync`, и проверить их перед выполнением запрошенного Bootstrap.

Поэтому в `guest.yaml` больше не требуется писать:

```yaml
bootstrap:
  capabilities:
    base: true
```

#### 3.6.1. `git`

`git` означает, что внутри гостевой ОС должен быть установлен и работать Git client.

Пример:

```yaml
bootstrap:
  - git
```

Эта возможность не выдаёт Git credential, не выбирает репозиторий и не клонирует произвольный repository.

Доступ к приватному репозиторию проекта является отдельной management-возможностью:

```yaml
management:
  - project_repo_read
```

#### 3.6.2. `docker`

`docker` означает, что непосредственно внутри гостевой ОС должен быть установлен и работать проектно поддерживаемый Docker Engine вместе с Docker Compose.

Пример:

```yaml
bootstrap:
  - docker
```

Для LXC это не заменяет требование выбрать профиль с feature `container-host`.

`container-host` означает, что LXC технически подготовлен для запуска вложенного container runtime. Bootstrap `docker` отдельно определяет, **нужно ли установить Docker** в конкретный экземпляр.

#### 3.6.3. `ansible`

`ansible` означает, что непосредственно внутри VM/LXC должен быть установлен и работать Ansible, в базовой реализации — `ansible-core`.

Пример:

```yaml
bootstrap:
  - ansible
```

Ansible устанавливается **не в Docker-контейнер и не через Docker**, а непосредственно в гостевую операционную систему развёрнутой VM или LXC.

`ansible` не требует `docker`.

`ansible` также не требует `git` как техническую dependency Bootstrap.

Если конкретному гостю нужны все три инструмента, они указываются независимо:

```yaml
bootstrap:
  - git
  - docker
  - ansible
```

Если Docker не нужен:

```yaml
bootstrap:
  - git
  - ansible
```

Bootstrap не устанавливает Semaphore, Gitea/Gogs, Jenkins, прикладные Docker Compose stacks или другие сервисы.

После минимальной первоначальной подготовки дальнейшая повторяемая конфигурация ОС и приложений должна выполняться отдельным provisioning-контуром, прежде всего Ansible.

### 3.7. Параметры профилей VM и LXC

Параметры этого раздела задаются в `guests/defaults.yaml` внутри определения профиля.

Они описывают **тип и способ создания** класса гостей, а не индивидуальные настройки конкретной VM/LXC.

В индивидуальном `guest.yaml` гостя с `profile` эти параметры не повторяются.

Целевая модель профилей:

```yaml
profiles:
  debian-vm:
    type: vm
    template_vmid: 9000

  debian-lxc:
    type: lxc
    ostemplate: local:vztmpl/debian-13-standard

  debian-lxc-docker:
    type: lxc
    ostemplate: local:vztmpl/debian-13-standard
    features:
      - container-host
```

Имя профиля выбирается для удобства человека и может отражать ОС, тип гостя и назначение, например:

```text
debian-vm
debian-lxc
debian-lxc-docker
ubuntu-vm
ubuntu-lxc
ubuntu-lxc-docker
```

Машинная логика не должна определяться разбором имени профиля. Она определяется только его полями.

> **Статус реализации:** defaults, schemas, resolver и validator уже используют `type`, `template_vmid` / `ostemplate` и высокоуровневый список `features`. Низкоуровневые VM/LXC-флаги остаются только в старом `deploy-guest` до его переработки.

#### 3.7.1. `type`

**Тип:** enum:

```text
vm
lxc
```

`type` определяет технический тип объекта Proxmox, создаваемого этим профилем.

Пример VM:

```yaml
debian-vm:
  type: vm
  template_vmid: 9000
```

Пример LXC:

```yaml
debian-lxc:
  type: lxc
  ostemplate: local:vztmpl/debian-13-standard
```

Дополнительные особенности профиля не создают новые технические типы.

Например профиль `debian-lxc-docker` всё равно имеет:

```yaml
type: lxc
```

а способность запускать вложенный container runtime описывается через `features`.

#### 3.7.2. `template_vmid`

**Тип:** integer от `100` до `9999`.

Параметр допустим только для профиля с:

```yaml
type: vm
```

Он определяет VM-шаблон, из которого создаются VM этого профиля.

Пример:

```yaml
profiles:
  debian-vm:
    type: vm
    template_vmid: 9000
```

Для управляемой VM используется Full Clone как фиксированное правило проекта.

Отдельного параметра `clone` в целевой модели нет.

QEMU Guest Agent также является обязательной частью контракта каждой управляемой VM и не задаётся отдельным boolean-полем профиля.

BIOS/UEFI наследуется от выбранного VM template и отдельно профилем не переопределяется.

Если `template_vmid` совпадает с VMID создаваемого гостя, проверка должна завершаться ошибкой: VM не может клонироваться сама из себя.

#### 3.7.3. `ostemplate`

**Тип:** string — селектор семейства Proxmox `vztmpl`.

Параметр допустим только для профиля с:

```yaml
type: lxc
```

Он определяет семейство системного образа LXC.

Пример:

```yaml
profiles:
  debian-lxc:
    type: lxc
    ostemplate: local:vztmpl/debian-13-standard
```

В Git хранится стабильный селектор семейства, а не конкретное имя versioned-архива.

Допустимо:

```text
local:vztmpl/debian-13-standard
```

Не допускаются wildcard, `latest`, `13.x`, `tbd` и конкретное имя загруженного архивного файла.

Все LXC, создаваемые универсальным deploy-контуром, являются **unprivileged**.

Это фиксированная политика проекта, поэтому отдельного параметра `unprivileged` в целевом профиле нет.

#### 3.7.4. `features`

**Тип:** необязательный список высокоуровневых возможностей профиля.

`features` используется тогда, когда конкретному классу гостя требуется дополнительная техническая подготовка на уровне Proxmox.

Первая поддерживаемая feature:

```text
container-host
```

Пример:

```yaml
profiles:
  debian-lxc-docker:
    type: lxc
    ostemplate: local:vztmpl/debian-13-standard
    features:
      - container-host
```

`container-host` означает:

> LXC технически подготовлен для запуска вложенного container runtime.

Feature не означает, что Docker уже установлен, и не привязана к конкретному продукту Docker.

В текущей реализации для LXC она разворачивается во внутренние Proxmox-настройки:

```text
unprivileged = true
nesting = true
keyctl = true
```

При этом `unprivileged=true` является общей политикой всех управляемых LXC, а `nesting` и `keyctl` добавляются именно для `container-host`.

Низкоуровневые параметры `nesting` и `keyctl` не являются публичными полями профиля и не задаются по отдельности.

Если конкретному LXC действительно нужно установить Docker, это задаётся отдельно в индивидуальном госте:

```yaml
profile: debian-lxc-docker

bootstrap:
  - docker
```

Таким образом:

```text
profile feature: container-host
→ LXC способен корректно запускать container runtime

bootstrap: docker
→ Docker Engine действительно должен быть установлен внутри гостя
```

Если у LXC запрошен Bootstrap `docker`, выбранный профиль должен содержать `container-host`. Иначе проверка должна завершаться ошибкой.

Список `features` специально является расширяемым: в будущем в него могут быть добавлены другие высокоуровневые технические возможности профиля без публикации низкоуровневых Proxmox-флагов в пользовательский контракт.

## 4. Правила и ограничения

### 4.1. VMID и адресация

Для управляемых VM/LXC используются VMID/CTID в диапазоне:

```text
100–999
```

Функциональная нумерация применяется к новым объектам и помогает группировать роли.

Принятые диапазоны:

| Диапазон | Назначение |
|---|---|
| `100–199` | сеть и базовая инфраструктура |
| `200–299` | Home Assistant и автоматика |
| `300–399` | прикладные, DevOps- и AI-сервисы |
| `400–499` | мониторинг и обслуживание |
| `500–599` | видео и камеры |
| `600–699` | временные и экспериментальные VM/LXC |
| `900–999` | устаревшие и служебные объекты |

Функциональная нумерация не является причиной перенумеровывать уже работающую систему.

Для центральной management-сети `/16` адрес по умолчанию вычисляется так:

```text
VMID XYZ + A.B.0.0/16
→ A.B.X.YZ
```

Например:

```text
311 + 192.168.0.0/16 → 192.168.3.11
311 + 10.0.0.0/16    → 10.0.3.11
```

Если нужен осознанный индивидуальный адрес, в `guest.yaml` задаётся только bare IPv4:

```yaml
network:
  ipv4: 192.168.8.50
```

Префикс остаётся центральным и берётся из `defaults.network.subnet`.

Индивидуальный адрес не может совпадать с gateway, network, broadcast или адресом другого гостя. Адрес, не совпадающий с формулой VMID, является осознанным исключением и отмечается проверкой предупреждением.

### 4.2. Профиль и участие в развёртывании

Поле `profile` является единственным признаком того, что для гостя применяется универсальная модель развёртывания.

Если `profile` указан:

- профиль должен существовать в `guests/defaults.yaml`;
- из профиля определяется `type` и параметры VM/LXC;
- в исходном `guest.yaml` должны быть заданы обязательные индивидуальные ресурсы;
- разделы `vm` и `lxc` не задаются непосредственно в индивидуальном манифесте;
- resolver строит полное effective state;
- гость может быть передан `deploy-guest`.

Если `profile` отсутствует:

- объект может быть описан в Git;
- его тип не требуется фиксировать отдельным полем в `guest.yaml`;
- полное effective state общим resolver не строится;
- универсальный deploy-контур не должен считать такой объект управляемым.

Наличие `bootstrap` или `management` требует `profile`, потому что эти возможности относятся к управляемому контуру гостя.

Для гостя с `profile` значение `pve_management` наследуется из defaults и по умолчанию равно `true`.

При `pve_management: true` объект должен находиться в PVE pool `managed`; при `false` — не должен. Это правило относится только к PVE API/MCP-контуру AI и не изменяет management SSH.

### 4.3. Management

Для любого гостя с `profile` административный management SSH является фиксированным проектным контрактом:

```text
root:22
```

Пользователь и порт не являются индивидуальными параметрами `guest.yaml`.

Дополнительные management-возможности задаются списком:

```yaml
management:
  - ssh_identity
  - project_repo_read
```

Допустимы только:

```text
ssh_identity
project_repo_read
```

Если `management` отсутствует, никакие дополнительные management-возможности гостю не требуются.

Элементы списка независимы друг от друга.

`ssh_identity` означает собственную исходящую SSH identity гостя.

`project_repo_read` означает фиксированный read-only доступ к репозиторию проекта и не заменяет установку Git client.

Список задаётся только в индивидуальном `guest.yaml`, требует `profile`, не наследуется из defaults/profile и не содержит `true/false`.

Применение этих возможностей выполняется только после запуска гостя и появления проверенного административного SSH.

### 4.4. Bootstrap

Базовая техническая готовность гостя является обязанностью deploy-контура и не выбирается в `guest.yaml`.

Опциональный Bootstrap содержит только явно требуемые инструменты:

```text
git
docker
ansible
```

Эти три пункта независимы друг от друга.

В частности:

```text
ansible
≠ зависит от docker

ansible
≠ устанавливается внутри Docker
```

Ansible устанавливается непосредственно в гостевую ОС VM/LXC.

Если одному гостю нужны несколько инструментов, они перечисляются независимо в `bootstrap`.

Манифест не является интерфейсом для произвольных shell-команд, package lists, URL установщиков, секретов или прикладных сервисов.

Запуск гостя для Bootstrap является обязанностью deploy-контракта и не задаётся отдельным параметром в `guest.yaml`.

### 4.5. Правила профилей VM и LXC

Профиль типа `vm` обязан содержать `template_vmid`.

Управляемая VM:

```text
создаётся Full Clone
использует QEMU Guest Agent
наследует BIOS/UEFI от template
```

Эти правила не задаются отдельными полями профиля.

Профиль типа `lxc` обязан содержать `ostemplate`.

Все управляемые LXC создаются непривилегированными:

```text
unprivileged = true
```

Если LXC-профиль содержит:

```yaml
features:
  - container-host
```

deployer дополнительно включает необходимые низкоуровневые возможности Proxmox:

```text
nesting = true
keyctl = true
```

Эти флаги являются внутренней реализацией feature и не выставляются как самостоятельные параметры профиля.

Bootstrap `docker` для LXC разрешён только при наличии `container-host` в выбранном профиле.

Имя профиля не является машинным контрактом. Например суффикс `-docker` может использоваться для удобства человека, но resolver и validator должны опираться только на `type`, source-параметр и `features`.

## 5. Проверка состояния

### 5.1. Схемы и общий resolver

Используются три машинные схемы:

```text
schemas/guest.schema.yaml
→ исходный guest.yaml

schemas/guest-defaults.schema.yaml
→ guests/defaults.yaml и profiles

schemas/guest-effective.schema.yaml
→ итоговое effective state
```

Схемы проверяют форму данных, допустимые значения и обязательные зависимости, которые можно выразить декларативно.

Общий resolver:

```text
scripts/guest_config.py
```

отвечает за правила, требующие вычисления или объединения данных.

Validator и средство развёртывания должны использовать этот общий модуль, а не создавать собственную реализацию merge, адресации или Bootstrap dependencies.

### 5.2. Проверка исходного и итогового состояния

Проверка выполняется в двух стадиях.

Сначала проверяются исходные файлы:

- формат `guests/defaults.yaml`;
- формат конкретного `guest.yaml`;
- допустимые поля и значения;
- отсутствие запрещённых неизвестных полей.

После этого resolver строит effective state.

Затем проверяются:

- полнота итогового состояния;
- соответствие типу VM/LXC;
- обязательные ресурсы;
- сеть;
- management-контракт;
- параметры VM/LXC;
- корректность Bootstrap.

Некоторые проверки нельзя выразить только YAML-схемой и выполняются программно.

### 5.3. Проверка репозитория

Основная команда проверки:

```bash
python scripts/validate_repo.py
```

Она проверяет не только отдельный YAML-файл, но и взаимосвязи между объектами проекта.

В частности, проверяются:

- соответствие `vmid` и `name` имени каталога;
- уникальность VMID;
- корректность профилей;
- корректность management-сети;
- вычисляемые и явно заданные IP;
- отсутствие конфликтующих адресов;
- допустимость переопределений;
- требования к гостя с `profile`м;
- VM- и LXC-ограничения;
- корректность Bootstrap;
- отсутствие секретоподобных полей и private key material.

Ошибка проверки означает, что состояние нельзя считать корректным входом для автоматического развёртывания.

Предупреждение означает состояние, которое формально допустимо, но требует внимания, например осознанное отклонение IP от формулы VMID или избыточное переопределение общего значения.

## 6. Связанные документы

- [`300-overview.md`](300-overview.md) — общая роль гостевых систем и их жизненный цикл.
- [`../100-architecture/120-state-model.md`](../100-architecture/120-state-model.md) — общая модель требуемого и фактического состояния проекта.
- [`../200-pve/200-overview.md`](../200-pve/200-overview.md) — роль PVE-хоста и граница между хостом и гостевым контуром.
- [`../../guests/README.md`](../../guests/README.md) — структура каталога конкретных VM/LXC.
- [`../../schemas/guest.schema.yaml`](../../schemas/guest.schema.yaml) — машинная схема исходного `guest.yaml`.
- [`../../schemas/guest-defaults.schema.yaml`](../../schemas/guest-defaults.schema.yaml) — машинная схема общих настроек и профилей.
- [`../../schemas/guest-effective.schema.yaml`](../../schemas/guest-effective.schema.yaml) — машинная схема итогового effective state.
- [`../../scripts/guest_config.py`](../../scripts/guest_config.py) — общий resolver требуемого состояния.
- [`../../scripts/validate_repo.py`](../../scripts/validate_repo.py) — проверка исходного и итогового состояния репозитория.
- [`../../templates/README.md`](../../templates/README.md) — шаблоны и источники для создания VM/LXC.
