# Ansible

Общие inventory, roles и playbooks для повторяемого управления VM/LXC и Docker-hosts.

Структура:

- `inventory/` — группы и hosts;
- `roles/` — переиспользуемые роли;
- `playbooks/` — сценарии развёртывания и обслуживания.

Секреты и приватные ключи сюда не добавлять.
